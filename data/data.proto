syntax = "proto3";

package data;

option go_package = './datapb';

message userRequest {
    string test_name = 1;
}

message serverResponse {
    string response = 1;
}

service dataService {
    rpc ValidateRequest(userRequest) returns (serverResponse) {};
}
